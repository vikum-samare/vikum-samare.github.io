import { enContent } from '@/config';
import { PrivacyPage } from '@/components/PrivacyPage';

export default function EnPrivacyPage() {
  return <PrivacyPage content={enContent} canonical="/privacy/" />;
}
